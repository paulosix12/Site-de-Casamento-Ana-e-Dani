name: Deploy Site Casamento

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    uses: paulosix12/pipelines/.github/workflows/deploy-html.yml@main
    with:
      project-name: "anaedani"
      source-dir: "."
